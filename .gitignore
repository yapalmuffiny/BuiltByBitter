# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
out/
dist/
*.tsbuildinfo

# Secrets — NEVER commit these
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# macOS
.DS_Store

# Editor
.idea/
.vscode/*
!.vscode/extensions.json

# Electron cached user data (if ever placed in-repo during dev)
userData/

# AI agent files
.claude/
CLAUDE.md
CLAUDE.local.md
.cursor/
.aider*
.windsurf/

# Generated icon asset catalog (regenerated by `npm run build:icon`, embeds a
# build timestamp so it is non-deterministic; the deterministic build/icon.icns
# is committed as the static fallback).
build/Assets.car

# Local dev SQLite (drizzle-kit)
local.db
local.db-shm
local.db-wal
*.db
*.db-shm
*.db-wal
